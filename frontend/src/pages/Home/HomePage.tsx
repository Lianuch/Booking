import type { FC } from "react";
import MainForm from "../../components/MainForm/MainForm";

const HomePage: FC = () => {
  return (
    <div>
      <MainForm />
   </div>
  );
};

export default HomePage;
